{
  "name": "turbostock",
  "version": "0.0.0",
  "description": "Outil de gestion des stocks - version ultra simplifiée pour faire des exercies de test logiciel",
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter=nyan",
    "init": "node build/init-database.js && mocha --reporter=nyan && node build/init-database.js --quiet",
    "format": "npx prettier . --write"
  },
  "author": "Lucien Bill",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^9.4.3"
  },
  "devDependencies": {
    "mocha": "^10.3.0",
    "prettier": "3.2.5"
  }
}
